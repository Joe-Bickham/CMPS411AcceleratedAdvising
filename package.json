{
  "name": "accelerated-advising",
  "version": "1.0.0",
  "private": true,
  "description": "Root package to start the server from project root",
  "scripts": {
    "start": "node server/server.js",
    "server:start": "npm --prefix server start",
    "server:scrape": "npm --prefix server run scrape:it",
    "open": "start http://localhost:4000"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.65.0",
    "axios": "^1.13.2",
    "cheerio": "^1.1.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0"
  }
}
